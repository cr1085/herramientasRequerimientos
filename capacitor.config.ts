import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'herramienta-requerimientos',
  webDir: 'www'
};

export default config;
